# v0.4.4 Rollback Point

## Target

Rollback from v0.4.4 Standalone Runtime Consolidation to the verified published v0.4.3 baseline.

## Recovery source

- Backup ID: `backup_2206707b-dc7_ms4glqok`
- Label: `v0.4.4-pre-refactor-rollback`
- Created: 2026-07-28
- Baseline status: published v0.4.3
- Files: 62
- Verified reason: preserve the stable build before standalone runtime and build-pipeline changes.

## Rollback procedure

1. Preview the backup manifest and verify its hashes.
2. Restore `backup_2206707b-dc7_ms4glqok` to the project with full-file replacement.
3. Validate `index.html` and all referenced assets.
4. Republish the restored project to the stable project URL.
5. Run desktop, tablet and mobile smoke checks.
6. Confirm the visible version is v0.4.3 and the previous cache/service-worker files are restored.

## Rollback trigger

Use the rollback only if a release-blocking regression is confirmed after publication, such as failure to boot, broken offline recovery, invalid standalone/ZIP export, missing Moon/planet behavior or persistent browser runtime errors.

## Expected impact

Rolling back removes the v0.4.4 generated standalone architecture and returns to the v0.4.3 duplicated standalone runtime. User projects remain compatible with the v0.4.3 baseline rules.
